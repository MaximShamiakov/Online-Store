export const handlePlusItem = (id, onPlus) => {
  onPlus(id);
};

export const handleRemoveClick = (id, onRemove) => {
  onRemove(id);
};

export const handleMinusItem = (id, onMinus) => {
  onMinus(id);
};
